doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title #{pageTitle} - KStore
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css")
    style.
      body {
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #f8f9fa;
        font-family: 'Inter', sans-serif;
      }
      .error-container {
        text-align: center;
        padding: 40px;
        background: white;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.05);
        max-width: 500px;
        width: 100%;
      }
      .error-code {
        font-size: 100px;
        font-weight: 800;
        color: var(--tz-blue, #0048ff);
        line-height: 1;
        margin-bottom: 20px;
      }
      .error-icon {
        font-size: 50px;
        color: #ffc107;
        margin-bottom: 20px;
      }
      .btn-tz {
        background-color: var(--tz-blue, #0048ff);
        color: white;
        padding: 6px 16px;
        font-size: 13px;
        border-radius: 6px;
        font-weight: 500;
        transition: all 0.2s;
        border: none;
      }
      .btn-tz:hover {
        background-color: var(--tz-blue-dark, #0036cc);
        color: white;
        text-decoration: none;
      }

  body
    .error-container
      .error-icon
        i.fas.fa-exclamation-triangle
      .error-code 404
      h3.font-weight-bold.mb-3 Xin lỗi, trang bạn tìm không tồn tại!
      p.text-muted.mb-4 Quý khách có thể đã nhập sai địa chỉ hoặc trang đã bị xóa/di chuyển. Vui lòng quay lại trang chủ để tiếp tục mua sắm.
      a.btn.btn-tz(href="/")
        i.fas.fa-home.mr-2
        | Trở về Trang Chủ
